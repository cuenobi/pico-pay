# Use postgres/example user/password credentials
version: '3.9'

services:
  postgres:
    container_name: postgres
    image: postgres:latest
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=P@ssword1234
      - POSTGRES_DB=pico-pay
    ports:
      - "5432:5432"
    restart: always
